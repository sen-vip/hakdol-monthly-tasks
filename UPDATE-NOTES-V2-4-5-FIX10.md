# v2.4.5-fix10

- 직접추가 색상을 학사일정 블루와 겹치지 않는 올리브/라임 계열로 변경
- 학교 설정 후에도 상단에 `학교 설정 전`으로 보이는 상태 표시 오류 수정
- 업무카드의 `미완료` 상태 버튼 클릭 시 `해당없음`으로 전환하고 카드 전체가 비활성화되어 보이도록 복구
- 보기 필터를 업무분장 바로 아래 왼쪽 정렬로 고정
- 캐시 방지 버전값을 `v245-fix10`으로 변경
